"""
This module configuration is only used to toggle object
validation at the moment. Any other configuration can be stored
here as well.
"""
config = {
    # Determines whether an error should be raised when the
    # server returns an unknown field in a response.
    'strict_validation': True,
}
